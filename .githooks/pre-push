#!/bin/sh
# Full gate before anything leaves the machine: lint, types, tests.
# Enable with: npm run hooks:install   ·   Bypass with: git push --no-verify
echo "pre-push: running npm run verify ..."
npm run verify || {
  echo "pre-push: verify failed - push aborted." >&2
  exit 1
}
